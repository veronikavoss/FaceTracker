# Face Tracker 설정 가이드 (Configuration Settings Guide)

이 문서는 Face Tracker 프로그램의 GUI 제어판과 설정 파일(`facetracker_config.json`) 내 다양한 설정 요소들의 역할, 내부 동작 방식, 그리고 최적의 설정을 맞추기 위한 추천 값을 다룹니다.

---

## 1. 마우스 이동 및 감도 설정

### 🔹 민감도 X / Y (Sensitivity X / Sensitivity Y)
* **기본값**: `5.0` (범위: `0.0` ~ `10.0`)
* **역할**: 머리(코끝)를 가로(좌우) 혹은 세로(상하)로 움직일 때, 마우스 포인터가 화면에서 이동하는 속도의 곱셈 배율입니다.
* **작동 방식**:
  * 값이 높을수록 머리를 조금만 움직여도 마우스가 화면 끝까지 빠르게 도달합니다.
  * 값이 낮을수록 조작이 무거워지지만 정밀한 타겟 클릭이 가능해집니다.
* **조절 팁**: 일반적으로 와이드 모니터 환경에서는 가로축(X) 이동 반경이 더 넓으므로 X축 민감도를 Y축보다 약간 높게 쓰는 것이 편안할 수 있습니다.

### 🔹 가속도 (Acceleration)
* **기본값**: `1.0` (범위: `0.0` ~ `2.0` / `1.0`은 가속도 없음)
* **역할**: 머리를 움직이는 **속도**가 빠를수록 추가 감도를 자동으로 더해줍니다.
* **작동 방식**:
  * 머리를 천천히 움직일 때는 원래 민감도로 차분하게 움직이며, 멀리 있는 아이콘으로 마우스를 옮기기 위해 머리를 빠르게 "휙" 움직일 때만 순간적으로 속도를 곱절로 뻥튀기해 줍니다.
* **조절 팁**: 가속도를 `1.15 ~ 1.30` 사이로 적당히 주면, 머리를 많이 돌리지 않고도 먼 거리를 쉽게 가로지르면서도 가까운 거리에서는 세밀하게 조준할 수 있습니다.

### 🔹 내부 배율 (Internal Multiplier)
* **기본값**: `40.0` (범위: `0.0` ~ `80.0`)
* **역할**: 모션 델타 연산이 다 끝난 값에 마지막으로 한 번 더 곱해지는 기본 베이스 속도 배율입니다.
* **작동 방식**: 마우스 이동 엔진의 최종 아웃풋 스케일입니다.
* **조절 팁**: 개별 민감도 슬라이더를 만지기 전에 전체적인 감도 뼈대(전체 볼륨)를 크게 줄이거나 늘릴 때 사용합니다.

---

## 2. 노이즈 및 떨림 억제 설정

### 🔹 임계값 (Motion Threshold / 데드존)
* **기본값**: `0.14` (범위: `0.00` ~ `0.28`)
* **역할**: 머리가 정지해 있거나 웹캠 자체의 화질 자잘한 노이즈 때문에 마우스 포인터가 제자리에서 부르르 요동치는 지터링(Jittering)을 없애주는 '최소 움직임 데드존'입니다.
* **작동 방식**:
  * 머리의 실시간 이동 거리가 임계값의 **60% 미만**이면 마우스를 완전히 멈춘 상태(`0.0`)로 강제 고정합니다.
  * 임계값의 **60% ~ 100% 사이**의 모션은 부드러운 2차 감쇄 곡선을 적용해 갑작스러운 정지나 끊김 현상을 방지합니다.
* **조절 팁**: 마우스를 가만히 두고 싶은데 제자리에서 바들바들 흔들린다면 이 임계값을 조금씩 올려 조율하세요.

### 🔹 스무딩 (Smoothing)
* **기본값**: `0.20` (범위: `0.00` ~ `0.40`)
* **역할**: 마우스 이동 델타의 흐름을 지수 이동 평균(EMA) 필터로 매끄럽게 다듬어서 부드럽게 미끄러지는 느낌을 줍니다.
* **작동 방식**:
  * **값이 작을수록 (0에 가까울수록):** 필터 강도가 높아서 자잘한 잔진동이 완전히 억제되지만, 물리적인 반응 반응 속도(딜레이)가 다소 늘어납니다.
  * **값이 클수록:** 딜레이 없이 거의 즉시 반응하지만 머리의 자잘한 제어 오류나 손떨림이 여과 없이 마우스에 전달됩니다.
  * *동적 스무딩 알고리즘*: 마우스를 빠르게 확 날릴 때는 딜레이가 없도록 필터링을 잠시 약하게 걸고, 정밀하게 조준하기 위해 멈칫할 때는 필터링 강도를 최대로 높여 조준을 돕도록 튜닝되어 있습니다.
* **조절 팁**: 미세 진동으로 마우스 사용이 피로하다면 스무딩 수치를 천천히 낮추며 필터를 강화해 보시는 것을 권장합니다.

---

## 3. 편의 및 하드웨어 연동 설정

### 🔹 단축키 (Tracking Toggle Key)
* **기본값**: `F12` (GUI 상에서 자유롭게 변경 가능)
* **역할**: 전체화면 게임 중이거나 다른 작업을 하다가 헤드 마우스 트래킹을 즉각 정지 또는 재개할 수 있는 전역 토글 키입니다.

### 🔹 카메라 선택 (Camera Selection)
* **역할**: 컴퓨터에 장착되어 있는 웹캠 목록 중, 추적용 영상 입력에 사용할 하드웨어 포트를 드롭다운으로 선택합니다.

### 🔹 저조도 FPS 드롭 방지 (수동 노출 고정)
* **역할**: 주변 광량이 어두워질 때 웹캠 드라이버가 더 많은 빛을 모으기 위해 프레임 레이트(FPS)를 30 아래로 자동 강하하는 현상을 차단합니다.
* **동작**: 체크 시 카메라의 자동 노출 제어를 끄고 고정값 노출 모드로 셋팅하여, 조명이 어두운 환경에서도 카메라가 지원하는 최고 속도(60fps/90fps 등)를 유지하여 부드러운 조작감을 보존합니다. (단, 화면이 기본보다 어두워질 수 있습니다.)
